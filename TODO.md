# TODO

## Immediate Tasks
- [ ] Complete documentation generation for this session.
- [ ] Check `npm install` progress.
- [ ] Locate main camera rig in `src/index.html` to begin designing the 2D "corner of desk" layout.
- [ ] Setup a basic HTML/JS wrapper for importing `tmi.js` (Twitch integration).
- [ ] Determine best method to inject MediaPipe script tags into `src/index.html` without breaking A-Frame.

## Short-Term Fixes/Refactors
- [ ] Upgrade deprecated packages in `package.json` if they cause build failures.
- [ ] Consolidate the controller configuration in `src/index.html` to allow injecting external coordinate data (for MediaPipe).
